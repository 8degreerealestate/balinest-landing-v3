import { CrmInlineForm } from "@/components/site/CrmInlineForm";

/** Investment Guide report download — GoHighLevel CRM inline form. */
export function InvestmentGuideReportForm() {
  return <CrmInlineForm form="investment-guide" />;
}
