import { CrmInlineForm } from "@/components/site/CrmInlineForm";

/** Location Guide report download — GoHighLevel CRM inline form. */
export function LocationGuideReportForm() {
  return <CrmInlineForm form="location-guide" />;
}
